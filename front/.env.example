# Laravel API base URL (no trailing slash). Login uses /api/v1 (e.g. .../api/v1/auth/login).
# When running: back on port 8000, front on 5173
VITE_API_URL=http://localhost:8000
# Or set full API base including /api/v1:
# VITE_API_BASE_URL=http://localhost:8000/api/v1
