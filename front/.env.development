# Dev UI calls the live API directly (ensure CORS allows your dev origin on the server).
VITE_API_BASE_URL=https://back.crm-amazonltd.live/api/v1
# Optional: proxy /api during dev to live if you switch to same-origin paths later
DEV_API_PROXY_TARGET=https://back.crm-amazonltd.live
