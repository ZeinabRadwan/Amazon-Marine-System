# API origin (no trailing slash). Requests go to {VITE_API_URL}/api/v1 — ensure CORS allows your dev origin if needed.
# For local Laravel again: VITE_API_URL=http://127.0.0.1:8000 and leave DEV_API_PROXY_TARGET matching.
VITE_API_URL=https://back.crm-amazonltd.live
DEV_API_PROXY_TARGET=https://back.crm-amazonltd.live
