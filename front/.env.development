VITE_API_BASE_URL=/api/v1
# If you prefer direct calls (and your server CORS allows it), revert to: https://back.crm-amazonltd.live/api/v1
If you prefer direct calls (and your server CORS allows it), revert to: https://back.crm-amazonltd.live/api/v1

# Optional: proxy /api during dev to live if you switch to same-origin paths later
# DEV_API_PROXY_TARGET=https://back.crm-amazonltd.live
DEV_API_PROXY_TARGET=http://localhost:8001